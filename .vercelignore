content/
scripts/
docs/
agent-orchestration/
demo/
.agents/
# Anchored to root so it excludes the top-level migrations dir only —
# NOT src/lib/supabase/ (the client factories the app imports).
/supabase/
