node_modules
.next
out
build
coverage
playwright-report
test-results
pnpm-lock.yaml
demo
content
scripts

# Docs are domain writing, not code — keep author's style.
docs
agent-orchestration

# Installed agent skills (external authors, not ours to reformat).
.agents

# Database migration record (SQL + notes), not application code.
# Anchored to root so it does not also skip src/lib/supabase/.
/supabase

# Supabase-generated types — committed artifact, matches `gen types` output.
src/types/database.ts
